# Photo Grid API Configuration
PORT=8000
GRID_TARGET_SIZE=1200
GRID_JPEG_QUALITY=92
GRID_MAX_PHOTOS=10
GRID_DOWNLOAD_TIMEOUT=30
# Leave empty for auto-detect (prefers Cairo, falls back gracefully)
GRID_FONT_PATH=
